{
  "schema": "branch-required-checks/v1",
  "schemaVersion": "1.0.0",
  "branches": {
    "develop": [
      "guard",
      "fixtures",
      "session-index",
      "issue-snapshot",
      "Policy Guard (Upstream) / policy-guard"
    ],
    "main": [
      "pester",
      "vi-binary-check",
      "vi-compare",
      "Policy Guard (Upstream) / policy-guard"
    ]
  }
}
